# Copy to terraform.tfvars and fill in your values.
# terraform.tfvars is gitignored — never commit it.

google_api_key = "AIza..."
# image_tag    = "latest"   # or a commit SHA for immutable deploys
